import { ExecArgs } from '@medusajs/framework/types'

export default async function seed({ logger }: ExecArgs) {
  logger.info('Edge Medusa seed placeholder: create regions, sales channels, stock locations, and API keys from the Admin when DATABASE_URL is configured.')
}
